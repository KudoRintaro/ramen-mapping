<% if FavoriteShop.exists?(user_id: current_user.id, shop_id: shop.id) %>
 <%= link_to "お気に入り解除", favorite_shop_path(shop.id), method: :delete, class:"favorite-delete-button" %>
<% else %>
 <%= link_to "お気に入り登録", favorite_shops_path, method: :post, class:"favorite-post-button" %>
<% end %>