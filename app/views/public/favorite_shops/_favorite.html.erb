<% if FavoriteShop.exists?(user_id: current_user.id, shop_id: shop.id) %>
 <%= link_to "お気に入り解除", shop_favorite_shops_path(shop), method: :delete, class:"favorite-delete-button" %>
<% else %>
 <%= link_to "お気に入り登録", shop_favorite_shops_path(shop), method: :post, class:"favorite-post-button" %>
<% end %>