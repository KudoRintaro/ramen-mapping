<% if obj.errors.any? %>
 <div class="alert">
  <h2><%= obj.errors.count %>個の入力エラー</h2>
  <% obj.errors.full_messages.each do |message| %>
   <ul>
    <li><%= message %></li>
   </ul>
  <% end %>
 </div>
<% end %>